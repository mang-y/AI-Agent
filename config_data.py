"""
全局配置文件
============

本文件集中管理 AI Agent 项目的所有可配置参数，包括：
- 向量库 (Qdrant) 配置
- Embedding / Chat / Rerank 模型配置
- 检索相关参数 (召回数、重排数、混合检索权重)
- Redis 历史记录配置
- LangSmith 可观测性配置
- Agent 行为配置 (最大迭代轮数等)
"""

# ============================================================
# LangSmith 可观测性配置
# ============================================================
# LangSmith 通过环境变量自动启用，无需在代码中显式调用。
# 在 .env 文件中设置以下变量即可开启全链路追踪：
#   LANGSMITH_TRACING=true
#   LANGSMITH_API_KEY=lsv2_xxx
#   LANGSMITH_PROJECT=rag-agent-demo
#   LANGSMITH_ENDPOINT=https://api.smith.langchain.com
LANGSMITH_PROJECT = "rag-agent-demo"

# Agent 单次会话最大工具调用轮数（防止死循环）
AGENT_MAX_ITERATIONS = 10

# ============================================================
# 反思/自纠错配置
# ============================================================
# Agent 输出最终回答前的最大反思次数（0 = 关闭反思）
MAX_REFLECTIONS = 2

# 反思评估用的系统提示词（LLM 评估回答质量）
REFLECTION_SYSTEM_PROMPT = """你是一个严格的回答质量评估员。你的任务是评估 AI 助手对用户问题的回答是否合格。

请从以下维度评估回答质量：
1. **完整性**：回答是否充分回应了用户的问题？
2. **准确性**：回答内容是否有明显错误或自相矛盾？
3. **有用性**：回答是否对用户有实际帮助？

注意以下情况应判定为"不合格"：
- 回答明确表示"无法回答"、"找不到信息"、"不确定"但没有给出替代建议
- 回答明显偏离用户问题
- 回答过于简短或含糊，缺乏实质内容
- 工具调用失败后未做有效补救

请仅返回一个 JSON 对象，不要包含任何其他内容。格式如下：
{"is_satisfactory": true/false, "issues": ["问题1", "问题2"], "feedback": "给 AI 助手的改进建议"}

如果回答合格，issues 和 feedback 可以为空数组/空字符串。"""

# ============================================================
# 向量库 (Qdrant) 配置
# ============================================================
collection_name = "rag"
persist_directory = "./qdrant_data"

# Embedding 向量维度（DashScope text-embedding-v3 为 1024）
embedding_dim = 1024

# ============================================================
# 文本分割配置
# ============================================================
chunk_size = 500
chunk_overlap = 150
separators = ["\n\n", "\n", ".", "!", "?", "。", "！", "？", " ", ""]

max_spliter_char_number = 1000  # 文本分割阈值

# 相似度 K 值
similarity_threshold = 1  # 检索返回匹配的文档数量

# ============================================================
# 模型配置
# ============================================================
embedding_model_name = "text-embedding-v3"
chat_model_name = "qwen3-max"
rerank_model_name = "qwen3-rerank"

# ============================================================
# 检索参数
# ============================================================
RECALL_K = 10          # 向量 + BM25 各自召回的文档数
SIMILARITY_K = 3       # Rerank 后保留的最终文档数
RRF_K = 60             # RRF 融合算法的平滑常数

# ============================================================
# 会话配置
# ============================================================
session_config = {
    "configurable": {
        "session_id": "user_001",
    }
}

# ============================================================
# Redis 历史记录配置
# ============================================================
REDIS_HOST = "localhost"
REDIS_PORT = 6379
REDIS_DB = 0
REDIS_PASSWORD = None

# 历史记录存储后端: "redis" 或 "file"
# 当 Redis 不可用时自动降级为文件存储
HISTORY_BACKEND = "redis"
HISTORY_FILE_DIR = "./chat_history"

# 单个会话保留的最大历史消息条数（防止上下文过长）
MAX_HISTORY_MESSAGES = 20

# ============================================================
# Agent 系统提示词
# ============================================================
AGENT_SYSTEM_PROMPT = """你是一个智能 AI 助手，名字叫"小知"。你拥有多种工具能力，可以根据用户的问题自主选择最合适的工具来完成任务。

## 你的核心能力

1. **知识库检索 (rag_search)**：当用户询问知识库相关内容时，使用混合检索（向量 + BM25 + Rerank）从 Qdrant 向量库中查找相关文档片段。
2. **知识库问答 (rag_answer)**：当用户需要基于知识库内容获得完整回答时，先检索再生成，确保回答有据可依、避免幻觉。
3. **文档上传 (upload_document)**：当用户想要添加新知识到知识库时，将文本内容分块并存入向量库。
4. **列出文档 (list_documents)**：查看当前知识库中已有哪些文档。
5. **RAG 评估 (evaluate_rag)**：对 RAG 系统进行质量评估（检索指标 + 生成指标 + 幻觉率）。
6. **历史记录查询 (get_chat_history)**：查看当前会话的历史对话记录。
7. **清空历史 (clear_history)**：清空当前会话的历史记录。
8. **计算器 (calculator)**：执行数学计算。

## 工具使用策略

- **优先使用知识库**：如果用户的问题可能涉及已上传的文档资料，优先调用 `rag_search` 或 `rag_answer`，而不是凭记忆回答。
- **区分 search 与 answer**：
  - 用户只想"查资料/看看有哪些相关内容" → 用 `rag_search`
  - 用户想要"完整回答" → 用 `rag_answer`
- **评估场景**：当用户明确要求"评估系统效果"或提供评估数据集时，使用 `evaluate_rag`。
- **通用问题**：对于常识性问题（如天气概念、数学计算、通用知识），可以直接回答或使用 `calculator`。
- **多轮对话**：注意结合上下文理解用户意图，必要时先查询历史记录。

## 回答规范

- 回答要简洁、专业、有条理。
- 基于知识库回答时，明确标注信息来源。
- 如果工具返回的结果不足以回答问题，诚实告知用户并建议补充资料。
- 使用中文回答（除非用户使用英文提问）。
"""
